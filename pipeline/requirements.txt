# Core pipeline (fetch_worldbank, fetch_elo, build_interest, build_pedigree,
# build_workbook, model, run_all) uses only the Python 3 standard library.

# Optional: live Google Trends soccer-interest proxy (fetch_trends.py).
pytrends>=4.9
